# Deprecated Area 1 compatibility paths

Current chamber implementations live under `res://Core/Chambers/` and `res://Regions/Hushiro/`.

This directory contains temporary compatibility shims for imported resources that still reference the prototype `Areas/Area1` paths. Do not add new gameplay code here. The obsolete Event and MapScreen prototypes were moved to `res://Legacy/Area1/`.
